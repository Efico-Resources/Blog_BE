import { userReq, userRes } from '../api_contracts/RegisterController.ctrl.contract'

/**
 * For Signing Up New Users
 */
export const Register = (req: userReq, res: userRes) => {
    res.send("How");
}